<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.stone.core.repository.UserMapper">

    <select id="getUserById" parameterType="String" resultType="user">
        select * from user where id = #{id}
    </select>

    <select id="getUserByName" parameterType="String" resultType="user">
        select * from user where userName = #{userName}
    </select>

    <insert id="createUser" parameterType="user">
        insert into user (id, userName, passWord, status, registerTime, remark)
        values (#{id}, #{userName}, #{passWord}, #{status}, #{registerTime}, #{remark})
    </insert>

    <update id="updateUser" parameterType="user">
        UPDATE user SET userName=#{userName}, passWord=#{passWord}, status=#{status}, registerTime=#{registerTime}, remark=#{remark}
        WHERE id=#{id}
    </update>

</mapper>